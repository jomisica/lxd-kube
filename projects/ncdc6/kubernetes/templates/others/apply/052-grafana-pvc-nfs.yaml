apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-nfs-storage
  namespace: monitoring
  labels:
    app: grafana
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 500Mi

